# Set to "false" once the real backend is live, and point VITE_API_BASE_URL
# at it. See src/lib/api.js — every request already matches the
# Authentication API Contract, so no other code changes are needed.
VITE_USE_MOCK=true
VITE_API_BASE_URL=http://localhost:4000
